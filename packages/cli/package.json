{
  "name": "@twinstar-bosskills/cli",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "dev": "tsc -p tsconfig.json --watch",
    "build": "tsc -p tsconfig.json",
    "check": "tsc -p tsconfig.json --noEmit",
    "format": "prettier . --write src",
    "clean-db": "node dist/clean-db.js"
  },
  "dependencies": {
    "commander": "^14.0.1"
  },
  "devDependencies": {
    "@twinstar-bosskills/db": "workspace:*",
    "@types/node": "^24.9.2",
    "typescript": "^5.9.3"
  }
}
