{
  "name": "@twinstar-bosskills/db",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "dev": "tsc -p tsconfig.json --watch",
    "build": "tsc -p tsconfig.json",
    "check": "tsc -p tsconfig.json --noEmit",
    "format": "prettier . --write src",
    "codegen": "DATABASE_URL=mysql://bk:bk@172.99.0.2:3306/bosskills kysely-codegen --dialect=mysql --out-file=./src/generated.types.ts --camel-case"
  },
  "dependencies": {
    "@twinstar-bosskills/api": "workspace:*",
    "@twinstar-bosskills/core": "workspace:*",
    "dotenv": "^16.6.1",
    "find-config": "^1.0.0",
    "kysely": "^0.28.8",
    "mysql2": "^3.15.3"
  },
  "devDependencies": {
    "@types/find-config": "^1.0.4",
    "kysely-codegen": "^0.19.0",
    "typescript": "^5.9.3"
  }
}
